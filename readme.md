# Flask 

> Flask env configs
```bash
    FLASK_APP=app
    FLASK_CONFIG=Develop
    FLASK_ENV=development
```

> Flask commands ( run and database )
```bash
    flask run
    flask db init
    flask db migrate
    flask db upgrade
```